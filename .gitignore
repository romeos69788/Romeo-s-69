# Build artifacts
.pio/
.vscode/

# Secrets (real credentials)
secrets/*.env
secrets/platformio.local.ini
secrets/mqtt.env
secrets/wifi.env
display/secrets/

# Node
node_modules/
dist/

# OS
.DS_Store
Thumbs.db

# Env files anywhere
.env
.env.local
.env.*.local

# Large archives in Romeos Tsakas import (keep local, not on GitHub)
docs/archive/**/*.zip
docs/archive/**/*.rar
docs/archive/**/*.7z
docs/archive/**/*.iso
docs/archive/**/*.exe
docs/archive/**/*.dll

# Root Gerber zip dumps (keep local)
/Gerber_*.zip

# Full EEZ bitmap dumps (~27 MB each) — use panel/src/ui slim RGB565 instead
Othoni Levita/**/ui_image_*.c
