# Handoff - 2026-04-28

## Update 2026-05-01 (display v2 / `main_clean.cpp`)

- **Four placeholder full-screen panels** from the left column: HEATING → `HEATING CONTROL`, LIGHTS → `LIGHTING CONTROL`, HEAT PUMP → `HEAT PUMP CONTROL`, BLINDS → `BLINDS CONTROL` (English titles, centered, large font). **BACK** (bottom-right) returns to `ui_THERMOSTAT_1`.
- **Calendar sync with wall clock:** on each simulated minute tick, when time rolls **23:59 → 00:00**, `bump_calendar_at_midnight()` advances weekday, day, month, and year (including leap years) so the date line matches the on-screen clock.
- Build target unchanged: `pio run -e BOARD_VIEWE_UEDX80480070E_WB_A`.

---

Current state locked for next session:

- Screen is stable and clean (no major flicker).
- Background image is visible and matches design.
- Main controls work:
  - setpoint `+/-`
  - time/date edit fields and arrows
  - menu button taps are wired
- Wi-Fi icons are visible (current quality accepted for now).
- Footer marker is present: `CLEAN-V2  MOD ...`

What was intentionally reverted before stopping:

- Dynamic dial experiments (extra moving dot / arc overlays) were removed.
- UI returned to the stable baseline behavior.

Next session starting point:

1. Keep current baseline untouched.
2. Design a safe dynamic dial plan using split assets (base/cool/warm/dot) before implementation.
3. Implement in small steps with visual check after each step.

